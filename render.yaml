databases:
  - name: ems-postgres
    plan: free
    databaseName: emsnew
    user: emsuser

services:
  # ── Spring Boot Backend ────────────────────────────────────────────────────
  - type: web
    name: ems-backend
    runtime: docker
    plan: free
    dockerfilePath: ./EmployeeManagementSystem/ems-backend/Dockerfile
    dockerContext: ./EmployeeManagementSystem/ems-backend
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: ems-postgres
          property: host
      - key: DB_PORT
        fromDatabase:
          name: ems-postgres
          property: port
      - key: DB_NAME
        fromDatabase:
          name: ems-postgres
          property: database
      - key: DB_USER
        fromDatabase:
          name: ems-postgres
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: ems-postgres
          property: password
      - key: JWT_SECRET
        value: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B597
      - key: MAIL_USERNAME
        value: hrtektalis@gmail.com
      - key: MAIL_PASSWORD
        sync: false   # paste your Gmail app password in the Render dashboard
      - key: TZ
        value: Asia/Kolkata
      - key: CORS_ALLOWED_ORIGINS
        value: https://ems-frontend-3a3h.onrender.com
      - key: COOKIE_SECURE
        value: "true"
      - key: COOKIE_SAME_SITE
        value: None

  # ── FastAPI Chatbot ────────────────────────────────────────────────────────
  - type: web
    name: ems-chatbot
    runtime: docker
    plan: free
    dockerfilePath: ./EmployeeManagementSystem/ems-chatbot/Dockerfile
    dockerContext: ./EmployeeManagementSystem/ems-chatbot
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: ems-postgres
          property: host
      - key: DB_PORT
        fromDatabase:
          name: ems-postgres
          property: port
      - key: DB_NAME
        fromDatabase:
          name: ems-postgres
          property: database
      - key: DB_USER
        fromDatabase:
          name: ems-postgres
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: ems-postgres
          property: password
      - key: GROQ_API_KEY
        sync: false   # paste your Groq API key in the Render dashboard
      - key: ALLOWED_ORIGINS
        value: https://ems-frontend-3a3h.onrender.com,https://ems-backend-609x.onrender.com

  # ── React Frontend (Static Site — CDN, never sleeps) ──────────────────────
  - type: static
    name: ems-frontend
    rootDir: EmployeeManagementSystem/ems-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        value: https://ems-backend-609x.onrender.com
      - key: VITE_CHATBOT_API_URL
        value: https://ems-chatbot-gymx.onrender.com/api/chatbot
      - key: VITE_PARSER_URL
        value: https://ems-chatbot-gymx.onrender.com
      - key: VITE_SESSION_TIMEOUT_MS
        value: "900000"
